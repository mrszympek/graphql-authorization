# import User from './generated/prisma.graphql'

type User {
    id: ID!
    name: String!
}

type Query{
    user(id: ID!): User
}

type Mutation {
    signup(name: String!): User!
    deleteUser(id: ID!): User!
}
